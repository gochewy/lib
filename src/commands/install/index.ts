import installComponent from './install-component/install-component';
import installRoot from './install-root/install-root';
import installTemplate from './install-template/install-template';

export default {
  installComponent,
  installRoot,
  installTemplate,
};
